function findLargest(numbers){
    let largest = numbers[0];
    for(let i=0;i<numbers.length;i++){
        if(numbers[i]>largest){
            largest = numbers[i];
        }
    }
    return largest;
}
let numbers = [3,7,4,22,9];
let result = findLargest(numbers);
console.log(result);
